# Synthetic sample

180 days × 3 SKUs, seed 42. Not real customer data. Run `python -m retail.cli train --csv data/sample/daily_sales_demo.csv` to test the custom CSV path. The default `demo` command generates 420 days × 20 SKUs independently.
